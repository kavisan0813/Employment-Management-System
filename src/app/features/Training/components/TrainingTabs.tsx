import type { TrainingRecord } from "../types/training.types";

const statusStyle: Record<string, string> = { Ongoing: "bg-emerald-50 text-emerald-700", Completed: "bg-teal-50 text-teal-700", Upcoming: "bg-amber-50 text-amber-700", Overdue: "bg-rose-50 text-rose-700", Draft: "bg-slate-100 text-slate-700", "Pending approval": "bg-violet-50 text-violet-700" };
export function TrainingTabs({ records, onDetails }: { records: TrainingRecord[]; onDetails: () => void }) { return <div className="overflow-hidden rounded-2xl border border-border bg-card shadow-sm"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div><h2 className="font-bold">All trainings</h2><p className="mt-0.5 text-xs text-muted-foreground">Showing {records.length} of 148 trainings</p></div></div><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-muted/50 text-xs uppercase tracking-wide text-muted-foreground"><tr><th className="px-5 py-3">ID</th><th className="px-5 py-3">Training</th><th className="px-5 py-3">Trainer</th><th className="px-5 py-3">Department</th><th className="px-5 py-3">Participants</th><th className="px-5 py-3">Due date</th><th className="px-5 py-3">Progress</th><th className="px-5 py-3">Status</th><th className="px-5 py-3" /></tr></thead><tbody>{records.map((item) => <tr key={item.id} className="border-t border-border"><td className="px-5 py-4 font-mono text-xs text-muted-foreground">{item.id}</td><td className="px-5 py-4 font-bold">{item.title}<p className="mt-1 text-xs font-normal text-muted-foreground">{item.category}</p></td><td className="px-5 py-4">{item.trainer}</td><td className="px-5 py-4">{item.department}</td><td className="px-5 py-4">{item.participants}</td><td className="px-5 py-4">{item.dueDate}</td><td className="px-5 py-4"><div className="flex items-center gap-2"><div className="h-1.5 w-16 overflow-hidden rounded-full bg-muted"><div className="h-full bg-primary" style={{ width: `${item.progress}%` }}/></div><span className="text-xs font-semibold">{item.progress}%</span></div></td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${statusStyle[item.status]}`}>{item.status}</span></td><td className="px-5 py-4"><button onClick={onDetails} className="font-semibold text-primary hover:underline">View</button></td></tr>)}</tbody></table></div></div>; }
