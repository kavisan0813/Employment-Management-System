/* ─── Candidate Process ─── */
// Placeholder: Future component for candidate-side onboarding tasks
// (e.g., documents the candidate needs to submit, forms to fill, etc.)

export function CandidateProcess() {
  return (
    <div className="px-6 py-5">
      <h4 className="text-[11px] font-semibold text-muted-foreground uppercase tracking-[0.2em] mb-3">
        Candidate Process
      </h4>
      <p className="text-[12px] text-muted-foreground">
        Candidate-side tasks will appear here when backend integration is complete.
      </p>
    </div>
  );
}
