import { DEFAULT_ONBOARDING_TEMPLATES } from "./defaultOnboardingTemplate";

/** Demonstration-only template. Replace this array with API data when available. */
export const ONBOARDING_MOCK_TEMPLATES = DEFAULT_ONBOARDING_TEMPLATES;
