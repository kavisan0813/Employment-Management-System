/* ─── Upload Constants ─── */
// Placeholder for future upload configuration

/** Enterprise document upload cap. Kept deliberately small for local storage. */
export const MAX_UPLOAD_SIZE_MB = 2;
export const ALLOWED_EXTENSIONS = [".pdf", ".jpg", ".jpeg", ".png"];
export const UPLOAD_ENDPOINT = "/api/onboarding/documents/upload";
